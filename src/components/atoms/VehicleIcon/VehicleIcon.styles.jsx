import styled from 'styled-components'

const VehicleIconStyles = styled.div`
    align-self: flex-end;
    height: 18px;
    padding: 0 10px 1px 0;

    svg{
        height: 18px;
        width: auto;
    }
`

export default VehicleIconStyles;
